

## Codely Structured Memories

### User

### Feedback
- [2026-07-23 08:42:31] When modifying existing architecture modules, the user prefers directly replacing the old implementation rather than creating a parallel new one. Don't invent new names (e.g. "Krea2Edit") when the existing name ("Krea2") suffices — replace in-place.
- [2026-07-24 01:02:55] tldraw custom shape types (BaseBoxShapeUtil) work fine in this project — the earlier "Error" was caused by two bugs: (1) calling `useEditor()` inside the `component()` method, and (2) wrong method signature `component({ shape }: { shape: AudioShape })` — the correct signature is `component(shape: AudioShape)` (receives shape directly, not destructured from an object). **How to apply:** Use `component(shape: MyShape)` and `indicator(shape: MyShape)`. Store all data in shape props via `static props = { ... }` + `getDefaultProps()`. Never call `useEditor()` or rely on `editor` inside `component()`. Register the shape util via `shapeUtils: [...defaultShapeUtils, MyShapeUtil]` on BOTH `createTLStore()` and `<Tldraw>`.
- [2026-07-24 01:43:16] ComfyUI `INPUT_IS_LIST` must be `True` (boolean), NOT `(True, False)` tuple — ComfyUI rejects per-input tuple syntax. **Why:** User corrected this when I tried `INPUT_IS_LIST = (True, False)`. **How to apply:** Use `INPUT_IS_LIST = True` to enable list input. When True, ALL inputs become lists, so non-list inputs like `key` need `key[0]` extraction inside the function.
- [2026-07-24 03:44:31] ACEStep15.extra_conds: providing `reference_audio_timbre_latents` forces `is_covers=True` and `pass_audio_codes=False`, which bypasses the LM-generated audio_codes from SFT TextEncode and causes silent output. **Fix:** Do NOT inject `reference_audio_timbre_latents` into conditioning. Instead, use `model.set_model_unet_function_wrapper()` to intercept the sampling call and replace `refer_audio` (silence latent) in the `c` dict with the reference audio VAE latent. This preserves the audio_codes flow while injecting timbre guidance. **Why:** audio_codes (from Qwen3 LM) provide semantic structure; without them the model tokenizes arbitrary reference audio → degenerate output. **How to apply:** Node returns patched MODEL + passthrough conditioning. Wrapper signature: `fn(apply_model_fn, args) -> output` where `args["c"]` is a dict of raw tensors (after `cond_cat`).
- [2026-07-26 06:49:07] Selected Program cards in right sidebar: toggle (enable/disable) and drag-reorder had two bugs. (1) Toggle failed because the card's `onMouseDown` handler checks `closest('.prefab-list')` to skip interactive children — but the card's container was also `prefab-list`, so it always matched and returned early. Fix: renamed the Selected Programs container from `prefab-list` to `program-list` (CSS `.program-list` added alongside `.prefab-list`). (2) Drag-reorder showed forbidden cursor because `onDragOver` checked `e.dataTransfer.types.length === 1` — on Windows, browsers add internal drag types during dragover, so this check fails and `preventDefault()` is never called. Fix: removed the `length === 1` check, just check `types.includes('text/plain')`.
- [2026-07-27 05:50:58] PySide6 + ThreadingHTTPServer: Qt requires ALL GUI operations on the same thread that created QApplication. prompt_node's HTTP server uses ThreadingHTTPServer (each request = new thread), so calling PySide6 GUI code (e.g. capture panel) directly from an HTTP handler works the first time (creates QApplication on thread A) but silently fails on the second call (thread B tries to use thread A's QApplication). **Fix:** dedicated persistent Qt thread with queue+Event dispatch — `_qt_capture_thread_main()` creates QApplication once, loops on `_qt_capture_queue.get()`, does all Qt work, sets `_qt_capture_done`. Caller (`_capture_and_tag`) puts tagger on queue and waits on the Event. **How to apply:** any PySide6 GUI code in prompt_node HTTP handlers must go through this pattern, never call Qt directly from the handler thread.
- [2026-07-27 05:50:58] Tag parsing in prompt_node has two layers: (1) Frontend `parseStringToTags(str, allPrompts)` only handles bracket notation like `[deco] base` — it does NOT do chain-matching decomposition. (2) Backend `SnapshotPromptNode._parse_raw_prompt(raw_text)` does the full chain-matching: splits by comma, then for each segment tries to decompose multi-word prompts into decorations + base via tag→prompt resolution (e.g. "solo focus" → "[solo] focus"). **How to apply:** when processing tagger/caption output (free-form text that may contain multi-word prompts), always call `_parse_raw_prompt` on the backend first, return the parsed bracketed strings array, then frontend uses `parseStringToTags` on each. Never split tagger output by comma and call `parseStringToTags` directly — multi-word prompts like "solo focus" won't match.
- [2026-07-27 06:33:47] assets_node.py uses `os.path.dirname(__file__), "..", "..", "data", ...` for data paths — that's TWO levels up from `nodes/`, reaching OUTSIDE the kolid-comfy package to `F:\ComfyDB\custom_nodes\data\` (not `kolid-comfy\data\`). prompt_node.py uses ONE level up (`..`) reaching `kolid-comfy\data\`. When referencing snapshots/assets from prompt_node that were created by assets_node, must use `../..` to match. **How to apply:** always check the number of `..` in path joins when sharing data dirs between node modules — they may resolve to different directories.
- [2026-07-28 07:17:47] When building interactive web UI nodes with multiple iframes (mask/prompt/etc), iframes MUST be always-mounted (use `display: none` to hide, not conditional rendering `{tab === 'x' && <iframe/>}`). Conditional rendering unmounts the iframe, losing its state and breaking postMessage communication. Also: when switching the image displayed in a mask iframe (e.g. after selecting a different history image), must send a `postMessage({type: 'reload-image'})` to the mask iframe — `server.mask_server.set_image()` only updates the backend, the browser iframe doesn't know the image changed.
- [2026-08-06 03:36:01] ComfyUI sub-graph execution pattern: CANNOT use `/prompt` API to re-run sub-graphs from within a running node — ComfyUI is single-threaded, the new prompt queues behind the current one → deadlock. Instead, execute nodes directly in Python: instantiate class_def, collect inputs from upstream outputs or widget values, call FUNCTION, store outputs. Use topological sort (Kahn's/DFS) for execution order. BFS from Start downstream + from End's value inputs upstream to collect all sub-graph nodes. **Why:** user initially chose `/prompt` API approach, but it deadlocked. **How to apply:** for any node that needs to trigger sub-workflow execution while blocking (e.g. interactive sampler nodes), use direct Python execution, not `/prompt` API.
- [2026-08-06 09:26:11] ComfyUI subgraph virtual ports (2026-08-06): This ComfyUI version uses virtual negative IDs (e.g. -10, -20) as subgraph boundary ports, NOT graph/input or SubgraphNodeInput proxy nodes. Virtual port -10's output slot N corresponds to sg_data.inputs[N] (name/type/linkIds). Subgraph node's own inputs in parent graph have link=None (unconnected). To resolve virtual inputs at execution time: match by sg_data.inputs[slot].name and .type — PIPELINE_DATA → injected pipeline, IMAGE → pipeline.get_image(), MASK → pipeline.mask. **Why:** spent hours debugging before discovering proxy node types were wrong. **How to apply:** always check sg_data.inputs/outputs for boundary definitions; don't rely on graph/input or SubgraphNodeInput node types.
- [2026-08-06 09:26:16] Direct node execution gotchas (2026-08-06): When executing ComfyUI nodes directly in Python (bypassing execution engine): (1) INPUT_IS_LIST=True nodes need inputs wrapped as [value]; (2) _widgets_values mapping must skip forceInput types and any_type — only STRING/INT/FLOAT/BOOLEAN/combo are real widgets; (3) missing required inputs need defaults from INPUT_TYPES; (4) external nodes (not in sub_prompt) referenced by links must be found in extra_pnginfo and executed on-demand; (5) widget values from extra_pnginfo are in array order matching INPUT_TYPES widget-only entries (not forceInput). **How to apply:** always check forceInput flag, wrap lists for INPUT_IS_LIST, fill defaults for missing required inputs.
- [2026-08-06 10:27:54] ComfyUI widget mapping for direct execution (2026-08-06): Use `inspect.signature(func)` to get parameter names from node FUNCTION, then filter by INPUT_TYPES to separate widget params from link params (forceInput or non-widget types like MODEL/IMAGE/CLIP). Handle `control_after_generate: True` on INT inputs — frontend adds an extra dropdown value in widgets_values array not present in function signature, must skip it during mapping. Apply type conversion (INT→int, FLOAT→float, BOOLEAN→bool) after mapping. **Why:** widgets_values array from extra_pnginfo has no names, only positional values; control_after_generate causes offset if not handled. inspect.signature is the most universal approach — works for any node without hardcoding widget lists. **How to apply:** always use inspect.signature + INPUT_TYPES filtering for widget mapping; never assume widgets_values aligns 1:1 with INPUT_TYPES order.
- [2026-08-06 11:32:48] Interface widget value priority (2026-08-06): In _map_all_widgets, when a node input already exists as a _virtual_input dict placeholder, it must be overwritten with the actual widget value from _widgets_values. The old check `if name not in node.get("inputs", {})` skipped these because the key existed. Fix: check if existing value is a dict with _virtual_input key, and if so, replace with widget value. **Why:** subgraph internal nodes (e.g. PipelineGetPromptNode) have their own widgets (context_regex, strength, etc.) stored in _widgets_values, but these were being skipped in favor of unresolved virtual inputs. **How to apply:** always check for _virtual_input placeholders when mapping widget values, not just key existence.
- [2026-08-06 13:49:08] Interface node output persistence (2026-08-06): Use ByPassTypeTuple with pre-defined RETURN_TYPES (like Easy-Use forLoopStart) instead of dynamic addOutput/removeOutput. JS syncValueOutputs only renames+retypes existing outputs, never removes them. This fixes output port disconnection on workflow reload. **Why:** ComfyUI restores links based on RETURN_TYPES at load time; if outputs are dynamically created/destroyed, links are lost. **How to apply:** always use ByPassTypeTuple with fixed RETURN_TYPES for nodes with dynamic outputs that need link persistence.
- [2026-08-06 22:49:21] Interface package /api/package display (2026-08-06): Only show ports physically connected to InterfaceStartNode and InterfaceEndNode. start_ports built ONLY from `start_types` (Start node's port_types widget), end_ports ONLY from `end_types` (End node's port_types widget). Do NOT include sg_input_defs/sg_widget_values (subgraph internal params like context_regex, strength, seed) — user explicitly does not want subgraph internals shown in the interface tab. **How to apply:** when building the interface display, never merge subgraph input definitions with the Start/End node's own value ports.
- [2026-08-07 06:27:38] Interface subgraph node collection algorithm (2026-08-07): Use forward∩backward reachability — forward BFS from Start collects all downstream nodes, backward BFS from End collects all upstream nodes, intersection = nodes on paths from Start to End. This correctly excludes external nodes that consume Start's outputs but don't lead to End (e.g. easy anythingIndexSwitch). **How to apply:** never use plain downstream BFS from Start — it collects external consumers. Always intersect with backward reachability from End.
- [2026-08-07 10:33:02] Interface 系统术语澄清 (2026-08-07): "子图 (subgraph)" = ComfyUI 原生 subgraph 功能（subgraph 节点封装内部节点，有 proxy 边界端口 graph/input/output 或负 ID，存储在 workflow.definitions.subgraphs）。"Interface 图" = InterfaceStartNode 和 InterfaceEndNode 之间围成的区域，由 InterfaceExecutor 收集和执行。两者正交——Interface 图内可包含子图节点，子图内也可包含 interface 节点。
- [2026-08-07 12:09:03] Interface 注入原则 (2026-08-07): 值注入只发生在 Start 节点处（通过 _build_injections + start_types）。子图内部节点的输入要么通过父图连线解析、要么通过子图节点自身 widget 值解析，都没有就报错。不要在 _resolve_virtual 或 _execute_node 中"猜测"值（如从 Start 输出按类型匹配、从 PipelineData 提取组件、fallback get_pipeline() 等）。**Why:** 猜测式注入导致类型不匹配的值被偷偷传递，掩盖了 workflow 设计错误。**How to apply:** 删除所有非 Start 处的注入/coerce 逻辑；_resolve_virtual 找不到值就 raise ValueError。
- [2026-08-08 05:02:01] mask_node.html grow/detect bug (2026-08-08): Grow 和 Detect 按钮的 mask 结果直接画到了 ctx (display canvas) 而非 maskDataCanvas, 导致: (1) 画笔覆盖 grow 结果 (livePreviewStroke 恢复 snapshotCanvas 不含 grow 结果), (2) 多次 grow 失效 (maskDataCanvas 没更新), (3) displayAlpha 不生效。**Why:** 所有 mask 操作 (grow/detect/clear/full/draw) 必须写入 maskDataCanvas, 然后调 redrawDisplay()。**How to apply:** mask_node.html 中任何修改 mask 的操作都必须操作 maskDataCtx + redrawDisplay(), 绝不直接写 ctx。
- [2026-08-08 05:19:28] Detailer reference 按架构区分 (2026-08-08): Flux2Klein 使用 reference_latent=tmp_latent (目标 latent), 不传 reference_image. Krea2 使用 reference_image=resized_image (源图), 不传 reference_latent. Flux2Klein.get_conditioning 有 conditioning 缓存, 追加 reference_latents 前必须 deepcopy 避免 positive/negative 互相污染缓存.
- [2026-08-08 06:02:50] mask_node.html mask 同步+持久化机制 (2026-08-08): mask 不实时同步, 仅在切换出 mask tab 时同步 — App.tsx handleTabChange 从 mask→其它时 postMessage({type:'sync-mask'}) 到 mask iframe, mask_node.html 收到后调 syncMask() POST /mask. Confirm 按钮仍调 sendMask() (POST /mask + postMessage mask-confirmed, 不再自动关闭窗口). mask 持久化: reload-image 不清除 mask, 改为调 loadImage() 恢复. set_mask() 同时更新 _initial_mask. /image_data 只在 initial_mask 尺寸与 image 匹配时返回. 相同尺寸→mask 保留; 不同尺寸→clear(). Detector 面板新增 Strength 滑块 (0-1, 默认 1.0), 检测结果合并后乘以 strength. Tag 面板只要有 tagger 就显示 (hasTagger prop from config.has_tagger, 不再需要 tag 结果).
- [2026-08-08 06:11:21] window.close() 对 webbrowser.open() 打开的标签页无效, 不能用于 finish 流程. **Why:** SnapshotDetailerSamplerNode 的前端用 webbrowser.open() 打开, window.close() 不起作用导致用户以为卡住. **How to apply:** 所有通过 webbrowser.open() 打开的交互节点, finish 后改为显示全屏完成页面 (✓ Finished, You can close this tab.), 不要调 window.close().
- [2026-08-08 09:36:11] SnapshotDetailerSamplerNode mask crop bug 修复 (2026-08-08): 两条 mask 同步路径冲突导致 mask 被覆盖. (1) handleTabChange→sync-mask→handleMask (保留 alpha) 设 pipeline.mask. (2) tag tab useEffect→get-mask→/api/submit_mask (二值化 >0) 覆盖 pipeline.mask, 导致 crop_mask bbox 不同 → "只切一部分". 修复: 移除 tag tab 的 submit_mask 重复同步; _run_detailer 中 expand_mask 前统一二值化 (user_mask = (user_mask > 0).float()); next_pipeline.mask = user_mask (保留原始 mask 不用 expand+recover 后的). **Why:** 两条路径产生不同 mask 形状, 第二次运行用被覆盖的 mask. **How to apply:** mask 同步只走一条路径 (handleTabChange→sync-mask→handleMask), 不重复 submit_mask.
- [2026-08-19 00:39:16] SnapshotDetailerSamplerNode IS_CHANGED=nan: IS_CHANGED 返回 float("nan") 使 ComfyUI 认为节点永远变了, 每次都重新执行. 之前有完成护栏 (_last_finish_time + FINISH_COOLDOWN=3.0s, per-instance) 防止 finish 后自动重入, 已于 2026-08-19 移除. 现在 finish 后 ComfyUI 可立即重新执行节点 (用户可再次交互). **How to apply:** IS_CHANGED=nan + 无 cooldown = 每次队列执行都重新打开交互界面. 如果不需要自动重入, 在 ComfyUI 前端关闭 auto-queue.

- [2026-08-18 23:52:01] - [2026-08-18 23:48:29] SnapshotDetailerSamplerNode history 存储: add_history 保留 tensor 引用到 _history_tensors dict (key→tensor), base64 仅用于前端缩略图. get_history_image 优先返回 tensor 引用, 避免 base64→PIL→numpy→tensor 的无谓往返和精度损失. **Why:** 之前每张图存 base64 (1024²≈4MB/张, 20张=80MB), get_history_image 每次 decode. **How to apply:** history 限制 20 张时同步清理 _history_tensors 中的旧 key.
- [2026-08-18 23:52:05] - [2026-08-18 23:49:42] SnapshotDetailerSamplerNode 状态分离 (2026-08-18): Draw tab 和 Interface tab 各有独立状态. 后端: server.detail_status/detail_progress/detail_error 用于 Draw, server.interface_status/interface_progress/interface_error 用于 Interface. /api/status 返回两组状态字段. 前端: App.tsx 分 detailStatus/interfaceStatus 独立轮询 (tab==='draw' && detailStatus==='running' 才轮询 draw, tab==='interface' && interfaceStatus==='running' 才轮询 interface). _execute_interface 的 on_progress/on_sampler_progress 回调写入 interface_* 字段. **Why:** 之前共享 detailStatus 导致切 tab 看到上次另一功能的状态.
- [2026-08-19 08:53:19] - [2026-08-19 09:00:00] SnapshotDetailerSamplerNode model patch 隔离 (2026-08-18, 更新 2026-08-19): _run_pipeline_blocks 中每 block 的 enable_edit patch 不写回 next_pipeline.model (patched model 只用于本次 _ksampler). 2026-08-19 进一步: 每 block 执行前保存 _orig_enable_edit 和 _orig_ref_latents, 执行后 finally 块恢复, 防止多个 block 的 enable_edit/reference_latents 互相污染. **How to apply:** 任何在 _run_pipeline_blocks 中对 model/config/reference 的临时修改都不应持久化到 pipeline, 且每 block 需 save/restore.

- [2026-08-18 23:52:12] - [2026-08-18 23:50:42] SnapshotDetailerSamplerNode prompt 同步: handleRunDetailer 前调 syncPrompt() — postMessage({type:'sync-prompt'}) 到 prompt iframe, 等待 prompt-synced 回调 (3s 超时 fallback). **Why:** prompt 同步只在切 tab 时触发 (handleTabChange prompt→other), 如果用户切 tab 太快就点 Run Detailer, sync 可能还没完成, _parse_prompt 读到旧值. **How to apply:** 任何读取 prompt_server 状态的操作前都应确保 sync 完成.
- [2026-08-18 23:52:14] - [2026-08-18 23:48:43] InterfaceExecutor _extract_results list 支持 (2026-08-18): End 端口输出为 Python list 时, 遍历每个元素作为独立结果 (含 IMAGE/PIPELINE_DATA/其他类型), 每个结果获得独立递增编号. **Why:** ComfyUI 节点可能返回 batch IMAGE (list of tensors), 之前 list 被当作单个值整体处理. **How to apply:** _extract_results 用内部 _add_one() 闭包处理单个值, 外层检测 isinstance(val, list) 时遍历.
- [2026-08-19 08:53:22] - [2026-08-19 09:00:00] SnapshotDetailerSamplerNode mask_grow/mask_blur 全局参数 (2026-08-19 更新): mask_grow/mask_blur 从 per-block 参数提取为全局参数 (Pipeline Block 系统中只做一次 expand_mask). 仍在 INPUT_TYPES 中 (向后兼容 ComfyUI widget), 但 _run_pipeline_blocks 从 global_params 读取. 前端 Draw tab: "Mask Settings" 独立 section, onGlobalParamChange 同步到 server. _apply_params 仍处理 mask_grow/mask_blur 单独更新 (兼容旧前端).

- [2026-08-19 00:11:43] - [2026-08-19 00:10:27] TSX 模板字符串陷阱 (2026-08-19): 在 JSX 属性值 (如 style={{ border: `0.5px solid ${expr}` }}) 中使用反引号模板字符串会导致 TS 编译器误判 JSX 闭合标签, 报 "JSX element has no corresponding closing tag" + "Unterminated template literal". **Why:** TSX 解析器在 JSX 上下文中对反引号的处理与 JS 不同. **How to apply:** TSX 中所有 JSX 属性内的动态字符串拼接改用 'string' + variable 方式, 不要用反引号模板字符串.
- [2026-08-19 00:37:06] - [2026-08-19 00:25:00] SnapshotDetailerSamplerNode tab 自动跳转行为 (2026-08-19): (1) mask-confirmed (Confirm Mask 按钮) → 自动跳到 tag (有 tagger) 或 prompt (无 tagger), 仅当当前在 mask tab. (2) Run Tag 完成 → 自动跳到 prompt tab. (3) prompt-confirmed (Prompt Confirm 按钮) → 自动跳到 draw tab, 仅当当前在 prompt tab. (4) Context tab 选图不再跳转 (之前跳 mask tab, 已移除). (5) mask 轮询 (/api/has_mask) 不再自动跳转 — 之前每 500ms 检测到 mask 存在就自动跳 tag/prompt, 现在只更新 maskConfirmed 绿点指示器. **How to apply:** 所有自动跳转通过 postMessage 事件触发 (mask-confirmed/prompt-confirmed) 或 handler 内 setTab, 不用轮询自动跳转.
- [2026-08-19 00:56:06] - [2026-08-19 00:55:00] SnapshotDetailerSamplerNode postMessage mask-confirmed 竞态条件 (2026-08-19): handleTabChange 的 checkSyncDone 和全局 postMessage handler 都监听 mask-confirmed/prompt-confirmed. sync-mask 触发 sendMask → mask-confirmed 被 checkSyncDone 消费切 tab, 但全局 handler 也收到同一事件 → tabRef 尚未更新 (仍为 mask) → 又触发 setTab('tag'), 覆盖用户目标 tab. **Fix:** 引入 consumedMaskConfirmedRef/consumedPromptConfirmedRef ref 标记, checkSyncDone sync 完成时 set ref=true, 全局 handler 先检查 ref——已 consumed 则重置 ref 并跳过 auto-advance. 同时 syncingTabRef (ref 而非 state) 避免 useEffect 闭包陈旧. timeout fallback 加 syncDone 检查防重复执行. **How to apply:** 多个 listener 消费同一 postMessage 事件时必须用 ref 标记消费状态, 不能仅靠 state (闭包陈旧).
- [2026-08-19 00:56:09] - [2026-08-19 00:56:00] PipelinePackageNode subgraph 名称推断 (2026-08-19): _infer_node_title 之前回退到 node.type, 但 ComfyUI subgraph 节点的 type 是 UUID (如 15e0f8eb-...). 修复: _infer_node_title 接收 subgraph_by_type 参数, 当 type 匹配 subgraph UUID 时从 subgraph_by_type[uuid].name 获取子图名称. **How to apply:** 节点 type 可能是 subgraph UUID, 不能直接用作显示名称, 需查 subgraph_by_type.
- [2026-08-19 00:56:13] - [2026-08-19 00:57:00] _try_execute_external 错误传播 (2026-08-19): 之前 _try_execute_external 捕获异常后 return None, 错误只在 console print, 不传播到 HTTP 响应 → 前端只看到 "Failed to execute upstream node XXXX" 无详情. 修复: 改为 raise RuntimeError + traceback.print_exc(), 让异常传播到 /api/switch_pipeline 的 except 块, str(e) 返回给前端. 同时: (1) 未连接的 PIPELINE_DATA 输入通过 get_pipeline() 回调注入当前 pipeline; (2) 执行前打印所有输入参数类型便于诊断. **How to apply:** _try_execute_external 不应吞异常 return None, 应 raise 让调用方处理.
- [2026-08-19 01:13:44] - [2026-08-19 01:30:00] _try_execute_subgraph 虚拟负 ID 修复 (2026-08-19): ComfyUI subgraph 内部节点输入连接到虚拟负 ID (如 -10) 时, 之前只处理 graph/input proxy 节点类型, 虚拟负 ID 不是 proxy 节点 (不在 sg_nodes 中), 走到 else 分支创建 [prefix+origin, slot] 链接引用 → _evaluate_node 试图当作外部节点执行 → "node not found in workflow". 修复: 将虚拟负 ID 检测 (origin.startswith("-") and origin not in sg_node_id_set) 与 graph/input proxy 检测合并为同一分支, 都走子图边界输入解析. **How to apply:** subgraph 输入有两种形式: graph/input proxy 节点 或 虚拟负 ID, 两者必须统一处理.
- [2026-08-19 01:13:47] - [2026-08-19 01:25:00] _try_execute_subgraph _eval_count 初始化 (2026-08-19): _try_execute_subgraph 调用 _evaluate_node 前必须初始化 _eval_count, _eval_total, _eval_cache, _eval_stack 四个属性. 这些属性只在 execute() 方法中初始化, subgraph 执行路径绕过 execute() → AttributeError. **How to apply:** 任何直接调用 _evaluate_node 的路径 (不经过 execute()) 都必须先初始化这四个属性.
- [2026-08-19 01:13:51] - [2026-08-19 01:15:00] sampler_node 前端 UI 改为侧栏布局 (2026-08-19): tab 从水平顶部改为垂直左侧栏 (52px 宽), iOS 风格 SVG 线条图标替代文字 (stroke-based, 22px, strokeWidth 1.6, 圆角端点). tab 按钮组垂直居中 (justifyContent center + flex 1), Finish 按钮 (SVG 对勾) 固定底部. Mask 图标=棋盘格, Blend=沙漏形. 图片预览统一用 objectFit: contain (非 cover). **How to apply:** 新增 tab 时在 TabIcon 组件加 SVG case, tabs 数组用 icon 字段而非 label.
- [2026-08-19 01:30:36] - [2026-08-19 01:45:00] Pipeline switch lora_regex 更新机制 (2026-08-19): 切换 pipeline 时从 new_pipeline.config.get("architecture") 获取架构名, 更新 inst.lora_regex + inst.prompt_server.update_lora_regex(arch). update_lora_regex 方法 (prompt_node.py SnapshotPromptServer) 重新扫描 loras (_scan_loras with new regex) 并重建 _valid_lora_paths. 前端 handleSwitchPipeline 成功后 postMessage({type:'reload-lora-data'}) 到 prompt iframe. prompt_node AppShell.tsx reloadHandler 新增独立分支响应 reload-lora-data: 只调 loadLoraData() 重新获取 /lora_data, 不清空已有选择 (与 kolid-reload-data 区分, 后者清空所有状态). sample() 初始化时 lora_regex 为空则从 pipeline.config.architecture 推导. **How to apply:** 切换 pipeline 后 prompt iframe 需 postMessage reload-lora-data 刷新 lora 列表, 不能用 kolid-reload-data (会清空所有状态).
- [2026-08-19 01:30:41] - [2026-08-19 01:40:00] Pipeline switch 继承 image/latent/mask (2026-08-19): 切换 pipeline 时新 pipeline 的 image/latent/mask 可能为 None (子图只修改了 model/clip/vae/config, 不含 image). 修复: 切换前检查 new_pipeline.image/latent/mask 是否为 None, 若是则从 current_pipeline 继承. **How to apply:** PipelinePackageNode 收集的子图输出 PipelineData 可能只包含部分字段, 需从当前 pipeline 继承缺失的 image/latent/mask.
- [2026-08-19 07:31:53] - [2026-08-19 02:15:00] _try_execute_subgraph 嵌套子图展开 (2026-08-19): 子图内部可能有嵌套 subgraph 节点 (type 是 UUID). 之前 _evaluate_node 遇到 UUID type 直接 SKIP. 修复: _try_execute_subgraph 展开阶段检测 sn_type in subgraph_by_type 时, 调用 _expand_nested_subgraph 递归展开嵌套子图内部节点到 sub_prompt (前缀 parent_prefix + sn_id + "."), 并创建 _subgraph_output_aliases 映射 (处理 graph/output proxy 和虚拟负 ID 两种输出形式). 嵌套子图的边界输入 (proxy/virtual) 从父子图 links 解析或注入 get_pipeline(). **How to apply:** subgraph 展开必须递归处理嵌套 subgraph, 不能在 _evaluate_node 运行时按需展开 (节点 ID 在父图中找不到). 输出 alias 必须同时处理 graph/output proxy 和虚拟负 ID.
- [2026-08-19 07:31:57] - [2026-08-19 02:10:00] LoRA 路径缓存构建时机 (2026-08-19): _build_lora_cache() 之前在 4 个节点的 INPUT_TYPES 中调用 (ContextNode, ReferenceLatentNode, ReferenceImageNode, ReferenceContolNetNode), ComfyUI 每次 graph 更新/节点创建都调 INPUT_TYPES → 缓存频繁重建 (1673 loras 每次扫描). 修复: 移除 INPUT_TYPES 中的调用, 改为模块加载时 _build_lora_cache() 执行一次. **How to apply:** 不要在 INPUT_TYPES 中做耗时操作, ComfyUI 频繁调用该方法.
- [2026-08-19 08:12:02] - [2026-08-19 08:30:00] _build_widget_dict ComfyNode 新 API 兼容 (2026-08-19): ComfyUI 新版 ComfyNode API (comfy_extras/nodes_primitive.py) 使用 FUNCTION="EXECUTE_NORMALIZED" (签名 (*args, **kwargs)) 包装真实 execute 方法. _build_widget_dict 必须检测 EXECUTE_NORMALIZED 并改用 execute 方法签名. 参数过滤用 inspect.Parameter.kind: 保留 POSITIONAL_OR_KEYWORD/KEYWORD_ONLY/POSITIONAL_ONLY, 跳过 VAR_POSITIONAL(*args)/VAR_KEYWORD(**kwargs), 额外跳过 'self'. hidden 类别全部跳过 (系统注入, 不从 widgets_values 映射). **How to apply:** 不要按参数名过滤 (args/kwargs), 用 kind 判断; 检测 EXECUTE_NORMALIZED 并回退到 execute 签名.

### Project
- [2026-07-24 03:18:19] User is working on adding ACE-Step reference audio (refer_audio) support to kolid-comfy. The goal is to enable refer_audio → context_latents → DiT conditioning in ComfyUI, matching the ACE-Step-1.5 server pipeline.
- [2026-07-26 06:49:05] Program system in prompt_node: "Programs" are JavaScript code snippets stored in `data/prompt/applications.json` (file name kept for backward compat). Categorized dict with bg_image/display_mode/size_mode/preview support per category. Programs execute on FRONTEND via `useProgram` hook (`new Function()`) with live preview — filtered items show dimmed (opacity 0.3). **Debug**: inline Console panel in Edit Program modal (between Code and Programs columns), with Run/Clear buttons + iOS-style scrollbar. Error line highlighted in Monaco via `errorLine` prop (`deltaDecorations` + `revealLineInCenter`). **Shared helpers** exported from `useProgram.ts`: `buildAllTagsLookup(allPrompts)`, `enrichTagGroups(selectedTags, allTagsLookup)`, `buildLoraSelectionData(selectedLoras, loraSelections)` — used by both `useProgram` and `handleDebug` in AppShell. **Program-sourced items stripped** at start of each `useProgram` run (`baseTags = selectedTags.filter(g => g.source !== 'program')`) so gen_* items don't accumulate. Context vars (JS): `tag_groups`(TagGroup[] where TagGroup={tags: Tag[], strength: number, source: SourceMode}; Tag={name, prompt, category, decorations?, tags?}), `loras`, `prefabs` (merged: selection state + actual tag/lora data from prefab def, uses `tag_groups` field), `custom_prompts`, `prompts_data`, `all_tags`. **SourceMode**: `type SourceMode = 'normal' | 'parsing' | 'program'` — replaces `is_from_parsing: boolean`. `normal`=user-selected (cached), `parsing`=from prompt_parsing (not cached, purple), `program`=added by program execution (not cached, re-generated each run, cyan glow). **Program return format (decoupled)**: `{ filter_tag_groups, filter_loras, filter_prefabs, gen_tag_groups, gen_loras, gen_prefabs, custom_prompts }` — filters remove items, gens add items. All programs' filters/gens collected into accumulators, applied at end (not per-program). Sidebar renders `gen_*` arrays directly (not `resultPrefabs.filter`). **Context vars**: `prefab_context` (Prefab[]), `lora_context` (Lora[]), `tag_context` (TagGroup[]) — per-instance, toggled by `enable_*_context` on `ProgramData`, data on `SelectedProgramItem`/`SelectedProgramRef`. Sub-programs inherit parent program's context (each program builds its own context from its `ctxSource`, not from a single root program). **Builtin vars**: `prefab_builtin` (Prefab[]), `lora_builtin` (Lora[]), `tag_group_builtin` (TagGroup[]) — shared on `ProgramData` (not per-instance), no enable toggle needed (data present → passed). Edited in Edit Program modal via `prefabBuiltin`/`loraBuiltin`/`tagGroupBuiltin` tempCtx modes. UI columns: Tags Builtin (text input + flex-wrap chips), Loras Builtin (Lora cards), Prefabs Builtin (prefab cards) — styled like Edit Prefab sections. **Builtin display mode**: each builtin item has normal/display toggle (D button on card). Display-mode items show on selected program instance cards (read-only, no delete button, toggle active/inactive only). `*_builtin_display` arrays on `ProgramData`. **Per-instance context**: `SelectedProgramItem` AND `SelectedProgramRef` both carry `context_prefab_guids`/`context_lora_paths`/`context_tag_texts` + `context_*_inactive` arrays, independent from shared `ProgramData` (which only has toggles, no context data). Context edited via temp context (`prefabCtx`/`loraCtx`/`tagCtx` modes), saves to instance via `setSelectedPrograms`, not `saveProgram`. Context restored from cache/load_from_image with full field copy. `last_selected_programs` updated on `/select_prompt` (not just `selected_programs`). **Multi Program**: `multi_program` toggle in Edit Program — when enabled, clicking program card always adds (never removes), allowing duplicate instances with independent contexts. `toggleProgramActive`/`removeProgram` operate by index (not id) to handle duplicates. `source` field inline on each item (prompts=`{text, source}[]`, loras/prefabs have `source` key). `exclude_from_cache` no longer sent from frontend — backend derives it: prompt widget filters `source != 'normal'`, lora/prefab widgets filter `source == 'program'`. `filter_tag_groups`/`filter_loras`/`filter_prefabs` also persisted to `/select_prompt` → `cache_data` dict for load_from_image round-trip. On load_from_image, filter items are restored as `source: 'normal'` in `newTags`/`newLoras`/`newPrefabs` (deduped against result items) so `useProgram` can re-filter them and show them dimmed. Left panel: collapsible categories with program cards (cross-category drag supported). Right sidebar `selected-bar`: Selected Prefabs → Selected Loras → Selected Programs → Selected Prompts. Node inputs: `program_cache`(BOOLEAN) + `program`(STRING JSON). Backend endpoints: `/add_program`, `/update_program`, `/delete_program`, `/reorder_programs`, `/move_program_to_category`, `/update_program_category`, `/update_program_display_mode`. Programs support sub-program dependencies via `selected_programs` (recursive execution, cycle-guarded, sub-programs execute even if parent has no code). Monaco Editor via `@monaco-editor/react` (CDN-loaded, types from `program-ctx-types.d.ts` via Vite `?raw` import, 500px fixed height, overflow visible). **TagGroup refactored**: Tag={name,prompt,category,decorations?,tags?}; TagGroup={tags:Tag[], strength, source:SourceMode}. **Field renamed**: PrefabData/SelectedPrefabItem/SelectedPrefabRef `tags` → `tag_groups`. Migration script: `nodes/migrate_taggroup.py`.
- [2026-07-26 19:12:16] QwenEdit architecture module added at architecture/QwenEdit.py — implements TextEncodeQwenImageEditPlus logic for the pipeline system. PipelineEnableQwenEditNode sets config["enable_qwen_edit"] flag (no model patch needed). When enabled, get_conditioning() dispatches to arch_qwen_edit (takes priority over Krea2/Flux2Klein architecture). Uses reference.reference_latents directly as reference_latents (not re-encoded), plus VAE-encodes reference_image to 1024×1024. VLM images scaled to 384×384 with llama_template + "Picture {N}: <|vision_start|><|image_pad|><|vision_end|>" format.
- [2026-07-27 09:34:47] Lora slider feature: LoraSliderConfig type (enabled, min, max, step, min_name, max_name, reverse, marks: LoraSliderMark[]) on LoraSelectionData. LoraSliderMark = {value: number, label: string}. Persisted via backend file `data/prompt/lora_slider_configs.json` — `/lora_data` returns `lora_slider_configs`, `/update_lora_slider_configs` POST saves. `useApi.ts` loads/sets `loraSliderConfigs` state. Edit button is on LoraItem.tsx (lora browser card), NOT on Lora.tsx (selected card). AppShell keeps `loraSliderConfigs` state (from useApi, not local). When lora is toggled on, slider_config is copied from loraSliderConfigs into loraSelections. Lora.tsx sliderConfig prop = `sel?.slider_config ?? loraSliderConfigs[lora.file_path]`. In slider mode, slider renders on its own row below header (`.lora-card-slider-row` needs `position: relative; z-index: 1`). Slider input wrapped in `.lora-card-slider-wrap` (position relative, z-index 2) so marks (z-index 1) sit behind. `buildLoraSelectionData` in useProgram.ts must pass `slider_config` for persistence. Number inputs for min/max/step use string state (not number) to allow typing negative values — `parseFloat` only on save. Reverse toggle: slider uses `direction: rtl` CSS, labels swap sides. Slider fill: inline `linear-gradient(to right, #0a84ff {pct}%, rgba(255,255,255,0.2) {pct}%)`; reverse uses `100 - pct`. Marks render as 2px×8px white vertical bars on slider track, positioned by percentage; reverse mirrors positions.  Lora fingerprint system: `_compute_lora_fingerprint(meta, lora_file_path)` — uses `civitai.files[0].hashes` only (SHA256 > AutoV2 > AutoV1 > BLAKE3, format `hash:VALUE`). Falls back to lora file content hash (first 1MB + file size, format `file:HEX[:16]`) only if no civitai hashes. Computed during `_scan_loras`, stored in `self.lora_fingerprints[file_path] = fingerprint`. Used for BOTH slider_configs AND selected_loras tracking. slider_configs stored by fingerprint key, resolved via `_get_lora_slider_configs_by_filepath()` on read. selected_loras: `/select_prompt` converts file_path→fingerprint via `_convert_loras_to_fingerprint_keys()` before storing; widget cache and cache_data also store fingerprints. On read, `_resolve_selected_loras_by_fingerprint()` resolves fingerprint→file_path. All output paths use `_resolve_lora_file_path()`. metadata.json maintained by another software (can't write to it).
- [2026-07-27 10:20:39] Prefab persistence bugs fixed: (1) `/add_library_prefab` and `/update_library_prefab` stored tag_groups under key `'tags'` instead of `'tag_groups'` — backend couldn't find them on reload. Fixed to use `'tag_groups'`. (2) `has_content` check in `/add_library_prefab` rejected prefabs with only a name (empty arrays for tags/loras/prefabs, no image) — prefab never written to library.json, disappeared on reload. Removed `has_content` check entirely; now only requires lib_name + prefab_name. **How to apply:** when adding new prefab endpoints or fields, always use `'tag_groups'` key (not `'tags'`) to match the TagGroup refactor.
- [2026-07-28 01:29:19] Decoration system REMOVED (2026-07-27). All prompts are now valid decoration candidates in parsing — `decorations`, `mute_decorations` fields removed from types/UI/API. `getPromptDecorations` deleted. `selectPrompt` no longer enters decoration sub-context (simple toggle). `ensureCache` (JS) and `_parse_raw_prompt` (Python) use `allPromptKeys`/`all_prompt_keys` (universal set of all prompt texts) as the decoration matching pool. `buildAllTagsLookup` returns `{name, prompt, category, tags}` only. **How to apply:** Don't reference `decorations`/`mute_decorations` in new code. Existing data files still have these fields but they're ignored.
- [2026-07-28 06:41:02] SnapshotDetailerSamplerNode (snapshot_sampler_node.py) REFACTORED (2026-07-28): old phase-loop architecture replaced with event-driven tab-based architecture. Backend uses `queue.Queue` + `threading.Event`. Main loop: `while not finished: action = wait_for_action()` — handles `run_detailer` / `select_image` / `finish` / `window_closed`. New endpoints: `POST /api/run_detailer`, `GET /api/history`, `POST /api/select_image {key}`, `POST /api/finish {selected_key}`. Frontend (nodes/sampler_node/src/) uses tabs (Mask/Tag/Prompt/Draw/Context). Draw tab: left panel has inline params (no foldout), right area has status/results + Run Detailer button bottom-right. Progress bar via `comfy.utils.set_progress_bar_global_hook()` intercepting ComfyUI ProgressBar → `server.detail_progress`/`detail_current_step`/`detail_total_steps` exposed via `/api/status`. iOS-style UI: 0.5px borders, no iframe border, 48px header, tab color highlight. **Required setup:** `self.prompt_server.tagger = self.tagger` + `self.prompt_server.asset = self.asset` after creation; `asset` is an optional STRING input on INPUT_TYPES for Tag From Assets support. `/api/run_tag` returns `{tag, tags, custom}` — frontend sends `{type:'auto-tag', tag, tags, custom}` to prompt iframe; prompt_node AppShell.tsx handles `tags` array via `parseStringToTags` with `source: 'parsing'` (same as Tag From Image button), `custom` text to customPrompts, fallback raw `tag` to customPrompts.

- [2026-07-30 22:19:52] mask_node.html (nodes/web/mask_node.html): bottom-bar: Grow(slider)→Grow(btn)→|→Size(slider,unlimited)→Full→Clear→|→Display(slider 0–1, default 0.5)→|→Confirm Mask. Display Alpha controls mask visibility on canvas (separate from mask data). Architecture: `maskDataCanvas` (offscreen) stores true mask data at full alpha; display canvas shows it scaled by `displayAlpha` via `redrawDisplay()`. All operations (draw, Full, Clear, initial_mask, get-mask/sendMask/grow) operate on `maskDataCanvas`. Brush modes via radial gradient in `makeBrushGradient()`: Binary (uniform alpha=strength, hard edge), Linear (center×strength → edge×strength), Exponential (center=strength, edge=0, `alpha=strength*(1-t)^gamma`). Brush params: strength(0–1), center(0–1), edge(0–1), gamma(0.1–10). `get-mask` postMessage includes `brush_mode/strength/center/edge/gamma`. `/api/submit_mask` binary thresholds `>0→strength`, linear/expo keep alpha as-is. PS-style stroke: offscreen `strokeCanvas` at full opacity, `snapshotCanvas` saves pre-stroke state, `livePreviewStroke()` restores snapshot→composites stroke→`redrawDisplay()`. Right-click=eraser (`destination-out`). Brush panel (always visible) above Detector panel. PS-style Alt+Drag brush resize. Tabs: Mask/Tag/Prompt/Draw/Blend/Context. Blend tab: iframe (`blend_node.html` at `/blend_node.html`). Right side panel has BG/FG thumbnail selectors (click → modal → `blend-image-selected` postMessage back) + Mask Preview. Blend canvas shows live alpha-blend result using canvas compositing (`destination-in` for mask alpha, no per-pixel ImageData). Blend button sends `blend-data` postMessage directly (bgKey/fgKey/mask/brush params) — no `blend-get-data` roundtrip. App.tsx receives → POST `/api/blend` → refreshHistory. Same brush/eraser system as mask_node (Binary/Linear/Exponential + Strength/Center/Edge/Gamma, PS-style stroke with strokeCanvas/snapshotCanvas, Alt+Drag resize, wheel, touch, right-click=eraser with `destination-out`, cursor mode switching). `onSetContext` sets context without leaving tab. `current_context_key` + `/api/result` keys for blue border highlighting. `_switch_image` preserves mask if same H×W. PipelineDecodeNode `isinstance` checks for list image/latent. Finish multi-select. Prompt_node convert buttons + auto-tag removes old parsing tags.
- [2026-08-07 12:08:51] Interface Package system (updated 2026-08-07): InterfaceStartNode has `interface_name` (STRING). Both Start and End are input-driven (BranchSwitchesNode pattern). RETURN_TYPES use `ByPassTypeTuple` (Start=`("*",)`, End=`("*",)`) so JS-added dynamic outputs pass ComfyUI validation without IndexError. `port_types` is a hidden STRING widget NOT serialized to widgets_values in PROMPT — `infer_port_types()` in get_package reads it directly from node.inputs[] + links (same logic as JS updatePortTypesWidget). When port_types is empty (normal ComfyUI execution, not InterfaceExecutor), Start/End passthrough all non-None values. InterfacePackageNode takes `node_id` (STRING) + hidden `EXTRA_PNGINFO` → outputs PACKAGE with `sub_prompt`. SnapshotDetailerSamplerNode has optional `package` input + hidden `EXTRA_PNGINFO` + `PROMPT`. **Sub-graph execution:** ComfyUI is single-threaded — CANNOT submit new prompt from running node (deadlocks). Execute nodes directly in Python via InterfaceExecutor (lazy evaluation from End node). **Key gotchas:** (1) RETURN_TYPES must use ByPassTypeTuple for dynamic outputs; (2) hidden inputs injected as `[value]` lists; (3) PROMPT has flattened nodes, EXTRA_PNGINFO has original graph with `subgraphs[]`; (4) widget values already in `inputs` dict for PROMPT format. **How to apply:** use PROMPT for node extraction (flattened, no subgraph expansion needed); use EXTRA_PNGINFO only for frontend graph structure.

- [2026-08-07 12:08:40] InterfaceExecutor class (updated 2026-08-07): Extracted from SnapshotDetailerSamplerNode into interface_node.py. Callback-based design: `__init__(extra_pnginfo, on_progress, get_pipeline, get_image, get_mask, on_result_image, on_result_pipeline, on_sampler_progress)`. `execute(pkg, manual_values)` is main entry. Contains: _rebuild_package, _build_injections, _map_all_widgets, _build_widget_dict (inspect.signature based), _evaluate_node (lazy), _resolve_link, _eval_link, _get_output, _resolve_virtual, _try_execute_external, _extract_results. **Lazy evaluation (2026-08-07):** replaced _topo_sort + _execute_node with recursive `_evaluate_node(end_id)` — from End node backwards, only evaluating nodes whose outputs are actually needed. Supports ComfyUI `check_lazy_status` interface: nodes with this method get called with pending links as None, only the returned required inputs are evaluated. Uses `is_link()` from `comfy_execution.graph_utils`. Cycle detection via `_eval_stack`. **How to apply:** any node needing sub-graph execution can use InterfaceExecutor with its own callbacks.
- [2026-08-07 22:50:28] Interface prompt tab injection (2026-08-07): _execute_interface now creates injected_pipeline (copy of _current_pipeline) with __prompt_tab__ context entry containing prompt tab's loras/prompts (from _parse_prompt). get_pipeline/get_image/get_mask lambdas use injected_pipeline instead of _current_pipeline. PipelineGetPromptNode with context_regex=".+" matches __prompt_tab__ (SamplerContext with positive/negative/loras) alongside existing contexts — same merge pattern as _run_detailer.
- [2026-08-07 22:50:37] Interface result display + Finish preview (2026-08-07): Backend tracks interface_result_keys on SnapshotDetailerSamplerServer (reset per _execute_interface, appended in on_result_image callback). /api/status returns interface_result_keys. App.tsx builds interfaceResults: HistoryItem[] from keys+history on polling done. InterfaceTab renders result cards (160px, click→onSetContext, blue highlight for active). Finish dialog reworked to left/right layout: left=hover large preview (falls back to first selected), right=420px card grid with hover+click select.
- [2026-08-19 00:37:03] Interface per-port image injection (updated 2026-08-19): InterfaceExecutor.execute(pkg, manual_values, port_overrides) — port_overrides={port_num: value} 在 _build_injections 之后覆盖特定端口注入值, 优先于回调注入. _execute_interface 从 exec_options.image_keys ({port_num_str: history_key}) 读取, 为每个有 key 的 IMAGE 端口从 history 加载图片构建 port_overrides. Operation (default/crop) 和 crop_reserve 是卡片级 (per-interface), image_keys 是端口级 (per-port). 前端 InterfaceTab: 每个 IMAGE inject 端口显示 40×40 缩略图预览 (选中图片或 context 图片), 点击打开选择 modal, 旁边 X 按钮清除选择 (回到 context image). 选中时蓝色边框+选中图片名称; 未选中时显示 "Context Image" + context 图片预览. showImageSelect 结构改为 {ifaceIdx, portNum} | null. execOptions 结构: {operation, crop_reserve, image_keys: {portNum: key|null}}.

- [2026-08-08 03:49:35] Krea2 像素空间 fit 路径修复 (2026-08-08): architecture/Krea2.py 新增 _imgids_offset (stride-1 居中偏移位置 ID) 和 _fit_encode_image (像素空间 crop+resize 后 VAE 编码, 防 latent 空间 bilinear resize 模糊). apply_model_patch 新增 fit_mode/vae/pixel_state 参数, forward 闭包在采样时按目标分辨率用 _fit_encode_image 编码源图. get_conditioning 收集像素空间源图存入 pixel_state. ConfigKrea2EditNode 新增 fit_mode 选项 (fit/crop, 默认 fit). snapshot_sampler_node enable_edit 创建共享 pixel_state 传给主模型和 model_negative.
- [2026-08-08 03:49:37] SnapshotDetailerSamplerNode Draw tab Enable Edit (2026-08-08): Draw tab Sampling Parameters 新增 "Enable Edit" checkbox. enable_edit=True 时 _run_detailer 按 architecture (Krea2/Flux2Klein) apply_model_patch (含共享 pixel_state 给 model_negative), 并将 reference_image 从 resized_image (裁剪后) 改为 original_image (原始上下文图). 后端: snapshot_sampler_node.py 新增 enable_edit 到 INPUT_TYPES/sample()/server config/_apply_params/_sync_widgets. 前端: types.ts DetailerParams+ServerConfig 加 enable_edit:boolean, App.tsx 同步, EditPhase.tsx 加 checkbox. updateParam 类型需 string|number|boolean.
- [2026-08-08 04:14:04] SnapshotDetailerSamplerNode Draw tab Context Reference (2026-08-08): enable_edit 开启后显示子区域 (iOS toggle) 含 Context Reference toggle + Reference Image 选择器. /api/run_detailer 改为 POST body 传 context_reference_key. _run_detailer 新增 server 参数, 当 enable_edit+context_reference+key 时从 history 加载图片→VAEEncode→追加到 next_pipeline.reference.reference_latents. 前端: IOSToggle 组件 (36×22 绿/灰, 从 region_node IOSToggle.tsx 内联), EditPhase.tsx editSubSection 样式 (左缩进+左边线). reference_image 始终用 resized_image (crop后的), context reference 作为额外 reference_latent 注入.
- [2026-08-08 05:19:25] SnapshotDetailerSamplerNode _parse_prompt lora trigger words fix (2026-08-08): _parse_prompt 之前只从 selected_prompts 构建 prompt, 未包含 lora 触发词. 修复: 从 selected_loras 中每个 active lora 的 active_tags 字段收集触发词, 追加到 user_positive 末尾, 与 SnapshotPromptNode.process() 的 merged_prompt = cleaned_result + ", " + lora_trigger_words 逻辑一致. 跳过 active=False 的 lora.
- [2026-08-08 10:29:31] SnapshotDetailerSamplerNode 完整架构文档 (2026-08-08): docs/snapshot_detailer_sampler_architecture.md 包含全部功能点、数据流图、服务器架构、mask/prompt 同步机制、_run_detailer 流程、_execute_interface 流程、已知问题。3个子服务器: mask_server(~8080, image_node.py), prompt_server(~8500, prompt_node.py), main_server(8700-8800, MainHandler). 前端 SPA: App.tsx(状态+轮询) + EditPhase.tsx(7个tab: Mask/Tag/Prompt/Draw/Blend/Context/Interface).
- [2026-08-19 00:11:10] - [2026-08-19 00:10:27] PipelinePackageNode + Pipeline Tab (2026-08-19): PipelinePackageNode (interface_node.py) 接收 node_id (中枢节点) + hidden EXTRA_PNGINFO, 从 graph 分析该节点的所有 PIPELINE_DATA 类型输入连接, 收集上游节点 ID + 名称, 输出 PACKAGE dict {type:"pipeline", name, pipelines:[{name,node_id,origin_slot}], node_id}. 不执行上游——只收集 graph 结构. 实际 pipeline 数据在交互时通过 InterfaceExecutor._try_execute_external() 执行上游节点获取. SnapshotDetailerSamplerServer 将 packages 分为 interface_packages (有 sub_prompt) 和 pipeline_packages (type=="pipeline"). /api/config 返回 has_pipeline_package. /api/pipeline_package 返回 pipeline 信息. /api/switch_pipeline {package_idx, pipeline_idx} 执行上游节点→切换 _current_pipeline (保留 history)→更新 mask server 图片. 前端: PipelineTab 组件 (360px 固定宽度卡片, 水平滚动), 显示 pipeline 列表 + Switch 按钮, 当前活跃 pipeline 绿色高亮. Tab 在 pipelinePackages.length > 0 时显示. 前端类型: PipelineInfo {name, node_id}, PipelinePackageInfo {name, pipelines}. TSX 中避免在 JSX 属性内使用反引号模板字符串 (TS 解析器会误判), 改用字符串拼接.
- [2026-08-19 08:53:14] - [2026-08-19 09:00:00] Pipeline Block 系统已实现 (2026-08-19): Draw tab 的单一 Sampling Parameters 改为 Pipeline Block 链式执行系统. **后端**: _run_detailer 替换为 _run_pipeline_blocks(pipeline, user_mask, user_positive, user_loras, global_params, blocks, server). global_params 含 seed/mask_grow/mask_blur/context_regex. blocks 是 server.blocks 列表. expand_mask 只做一次 (全局 mask_grow/mask_blur), crop_mask 只做一次 (用第一个 Detailer block 的 crop_reserve). 每个 Detailer block: limit_pixels→VAEEncode→conditioning→_ksampler→VAEDecode, 中间 block 不 recover_size/recover_crop. 最后一个 block 采样后 recover_size+recover_crop 回原尺寸. 每 block 保存/恢复 config["enable_edit"] 和 reference.reference_latents (防跨 block 污染). Interface block 暂为占位 (chain 中跳过). server.__init__ 初始化 self.blocks (默认单个 Detailer block 从 INPUT_TYPES 值). _apply_params 处理 blocks 更新 (从第一个 Detailer block 同步 individual params for ComfyUI widget). /api/config 返回 blocks. **前端**: types.ts 新增 PipelineBlock{id,type,name,params,interface_index?} + DetailerBlockParams (无 mask_grow/mask_blur). App.tsx 用 blocks/maskGrow/maskBlur 状态替代 params. EditPhase Draw tab: Mask Settings (全局 mask_grow/mask_blur) + Pipeline Blocks 列表 (每 block 独立参数面板, ↑↓ 排序, ✕ 删除) + "+ Detailer"/"+ Interface" 添加按钮. updateBlockParam(blockId, key, value) 替代旧 updateParam. showRefSelect 改为 blockId|null.


### Reference
- [2026-07-23 23:40:11] [reference] tldraw assets_node frontend source lives at nodes/assets_node/src/ with Panel.tsx and App.tsx as main files. Build via `npm run build` in nodes/assets_node/ — output goes to nodes/web/assets_node.html (vite + vite-plugin-singlefile, inlines all JS/CSS). The vite config renames index.html → assets_node.html.
- [2026-07-24 03:18:21] [reference] ComfyUI-AceStep_SFT installed at F:\ComfyDB\custom_nodes\ComfyUI-AceStep_SFT — provides AceStepSFTModelLoader (→MODEL/CLIP/VAE), AceStepSFTTextEncode (→CONDITIONING), AceStepSFTGenerate (sampler+VAE decode, has latent_or_audio input for src_audio/cover/repaint), AceStepSFTLoraLoader, AceStepSFTMusicAnalyzer. Missing: refer_audio → context_latents conditioning injection.
- [2026-07-24 03:18:23] [reference] ACE-Step-1.5 server repo cloned to .codely/ACE-Step-1.5 for analysis (from https://github.com/ace-step/ACE-Step-1.5). Key files: handler.py (AceStepHandler = Mixin composition), core/generation/handler/conditioning_embed.py (infer_refer_latent: VAE encode refer_audio → context_latents), conditioning_masks.py (chunk_masks + src_latents for repaint), generate_music_request.py (_prepare_reference_and_source_audio: refer vs src distinction).
- [2026-07-24 03:36:14] [reference] ACEStep15 ComfyUI core model at C:\Users\Klein_Blue\AppData\Local\Programs\ComfyUI\resources\ComfyUI\comfy\ldm\ace\ace_step15.py — AceStepConditionGenerationModel with DiT + TimbreEncoder + ConditionEncoder. extra_conds in model_base.py L1826 reads `reference_audio_timbre_latents` (top-level cond_dict key, list of tensors, does [-1][:,:,:noise_len]) → `refer_audio` CONDRegular → DiT forward uses it for timbre_encoder + tokenizer.tokenize() → context_latents.
- [2026-07-25 04:53:32] prompt_node frontend build: `cd nodes/prompt_node; npm run build` (tsc + vite + rename-output → nodes/web/prompt_node.html). Backend syntax check: `python -c "import ast; ast.parse(open('nodes/prompt_node.py', encoding='utf-8').read())"`. Both must pass after changes.
- [2026-07-25 08:27:24] prompt_node uses Monaco Editor (via `@monaco-editor/react`) for Program code editing in Edit Program modal. Monaco loads from CDN at runtime (not bundled into single-file build), keeping HTML at ~397KB. Component: `src/components/ProgramCodeEditor.tsx`. Theme: custom `kolid-dark` (#1c1c1e background). JS syntax highlighting + autocomplete, semantic validation disabled (programs are snippets).
- [2026-07-28 06:32:55] sampler_node frontend source at nodes/sampler_node/src/ (NOT nodes/snapshot_sampler_node/). Build: `cd nodes/sampler_node; npm run build` (tsc + vite, rename done by vite plugin in @kolid/ui-utils/vite → outputs nodes/web/sampler_node.html). Main files: App.tsx (orchestrator), components/EditPhase.tsx (tab UI), components/AdvancedSettings.tsx. Shared vite config via createViteConfig({appName, outDir, base}) from @kolid/ui-utils/vite.
- [2026-08-06 07:00:04] LiteGraph subgraph serialization (2026-08-06): Subgraphs are stored in `workflow["definitions"]["subgraphs"]` (NOT `workflow["subgraphs"]`). Each subgraph has {id, name, nodes, links, inputs, outputs}. A subgraph node in workflow.nodes[] has `type` = the subgraph's UUID. Internal proxy nodes are `graph/input` and `graph/output` (NOT `SubgraphNodeInput`/`SubgraphNodeOutput`). Internal links may be dict format (keyed by id) not list format — must normalize. SubgraphNodeInput output slot = parent subgraph node's input slot; SubgraphNodeOutput input slot = parent output slot. **How to apply:** read from `workflow.definitions.subgraphs`, normalize links, match proxy node types `graph/input`/`graph/output`.

